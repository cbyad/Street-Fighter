package com.cps.display;

import javafx.scene.input.KeyEvent;

public interface movable {

	/**
	 * methode listener pour gestion deplacement d'un hitbox dans une scene
	 * @param e
	 */
	public void keyPressedAction(KeyEvent e );
	
}
